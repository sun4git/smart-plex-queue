# Smart Plex Queue - Webhook Listener (systemd unit)
#
# Install:
#   sudo cp smart-plex-queue-listener.service /etc/systemd/system/
#   sudo systemctl daemon-reload
#   sudo systemctl enable --now smart-plex-queue-listener.service
#
# Before installing, edit User= and WorkingDirectory= below to match your
# box, and confirm the ExecStart= python3 path with `which python3`.

[Unit]
Description=Smart Plex Queue - Webhook Listener
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=suneel
WorkingDirectory=/home/suneel/sunwork/apps/smart-plex-queue
Environment=PYTHONUTF8=1
ExecStart=/usr/bin/python3 -u queue_listener.py 8000
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
